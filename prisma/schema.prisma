generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Student {
  id        String   @id @default(cuid())
  name      String
  class     String
  order     String
  createdAt DateTime @default(now())
}
